import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:preppro/features/recipes/recipe_detail_page.dart';

void main(){
  testWidgets('Recipe not found path shows message', (WidgetTester tester) async {
    // Skipped for now: depends on RecipeLoader.load() which needs asset/mocking setup.
    return;
  });
}
