import 'package:flutter_test/flutter_test.dart';

void main(){
  // Skipped: depends on RecipeLoader.load(); enable with asset/mocking setup.
  testWidgets('Recipe not found path shows message', (WidgetTester tester) async {
  }, skip: true);
}
